# Screen Description: Certification (Content Only)

### Main Content Area
The central workspace is vertically stacked into three distinct white panels (cards).

#### Panel A: Configuration & Visualization (Top)
A large container for setting up parameters and viewing results.

* **Input Fields (Top Section):**
    - Use (certification-form.validator.md)[/certification-form.validator.md]
    * **Aggregation block:** Dropdown menu with placeholder text "Select" (Arrow indicator on right).
    * **Select PpS:** Dropdown menu with placeholder text "Select" (Arrow indicator on right).
* **Parameter Displays (Middle Section):**
    * Three horizontally aligned data containers:
        1.  **PDG (MW)**: Empty input/display field.
        2.  **ActivationPower**: Empty input/display field.
        3.  **Trend**: Empty input/display field.
* **Visualization Area (Center):**
    * A large whitespace area containing a Line Chart, generate sume dummy data and update them every two second.

#### Panel B: Action Control (Middle)
A narrow horizontal bar containing a primary action button.

* **Button:** **"Start test"** (Centered).
    * *State:* Appears disabled/inactive (greyed out).

#### Panel C: Assistance (Bottom)
A narrow horizontal bar providing access to documentation.

* **Button:** **"Show instructions"** (Left aligned).
    - On click call (show-instructions.function.md)[/show-instructions.function.md]
    * *State:* Active (filled background).